<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_all_songs">

    <fragment
        android:id="@+id/nav_all_songs"
        android:name="com.safalfrom2050.projectaudio.ui.allsongs.AllSongsFragment"
        android:label="@string/title_all_songs"
        tools:layout="@layout/fragment_all_songs">
        <action
            android:id="@+id/action_nav_all_songs_to_nav_albums"
            app:destination="@id/nav_home" />
    </fragment>

    <fragment
        android:id="@+id/nav_home"
        android:name="com.safalfrom2050.projectaudio.ui.home.HomeFragment"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_home" />

</navigation>
